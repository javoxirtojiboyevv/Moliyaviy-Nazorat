import Link from 'next/link'
import { AuthFrame } from '@/components/auth-frame'
import { login } from './actions'
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <AuthFrame title="Xush kelibsiz" subtitle="Akkauntingizga kirib qarzlaringizni boshqaring." footer={<span>Akkauntingiz yo‘qmi? <Link href="/register">Ro‘yxatdan o‘ting</Link></span>}><form action={login} className="auth-form"><input type="hidden" name="next" value={q.next||'/dashboard'}/><label><span>Email</span><input name="email" type="email" autoComplete="email" required placeholder="siz@email.uz"/></label><label><span>Parol</span><input name="password" type="password" autoComplete="current-password" required placeholder="••••••••"/></label><div className="auth-row"><span/><Link href="/forgot-password">Parolni unutdingizmi?</Link></div>{q.message&&<div className="alert alert-success">{q.message}</div>}{q.error&&<div className="alert alert-danger">{q.error}</div>}<button className="btn primary full">Kirish</button></form></AuthFrame>}
