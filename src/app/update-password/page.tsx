'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { createClient } from '@/lib/supabase/client'
export default function UpdatePassword(){const [p,setP]=useState(''),[c,setC]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();if(p.length<8)return setError('Parol kamida 8 belgi bo‘lishi kerak.');if(p!==c)return setError('Parollar mos emas.');setBusy(true);const {error}=await createClient().auth.updateUser({password:p});setBusy(false);if(error)return setError(error.message);router.replace('/dashboard')}return <AuthFrame title="Yangi parol" subtitle="Akkauntingiz uchun yangi parol kiriting."><form onSubmit={submit} className="auth-form"><label><span>Yangi parol</span><input type="password" value={p} onChange={e=>setP(e.target.value)} required/></label><label><span>Parolni takrorlang</span><input type="password" value={c} onChange={e=>setC(e.target.value)} required/></label>{error&&<div className="alert alert-danger">{error}</div>}<button className="btn primary full" disabled={busy}>{busy?'Saqlanmoqda…':'Parolni yangilash'}</button></form></AuthFrame>}
function AuthFrame({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <main className="auth-page"><div className="auth-card"><a href="/" className="brand auth-brand"><span className="brand-mark">QN</span><span><b>Qarz Nazorati</b></span></a><h1>{title}</h1><p>{subtitle}</p>{children}</div></main>}
