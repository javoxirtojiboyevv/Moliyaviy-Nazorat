import Link from 'next/link'
import { AuthFrame } from '@/components/auth-frame'
import { register } from './actions'
export default async function Register({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <AuthFrame title="Akkaunt yarating" subtitle="Bir daqiqada ro‘yxatdan o‘ting va moliyaviy nazoratni boshlang." footer={<span>Akkauntingiz bormi? <Link href="/login">Kirish</Link></span>}><form action={register} className="auth-form"><label><span>Ism</span><input name="fullName" required autoComplete="name" placeholder="Ismingiz"/></label><label><span>Email</span><input name="email" type="email" required autoComplete="email" placeholder="siz@email.uz"/></label><div className="two"><label><span>Parol</span><input name="password" type="password" minLength={8} required autoComplete="new-password"/></label><label><span>Takrorlang</span><input name="confirm" type="password" minLength={8} required autoComplete="new-password"/></label></div>{q.error&&<div className="alert alert-danger">{q.error}</div>}<button className="btn primary full">Ro‘yxatdan o‘tish</button><small className="terms">Davom etish orqali shaxsiy moliyaviy ma’lumotlaringiz faqat o‘z akkauntingizga bog‘lanishiga rozilik bildirasiz.</small></form></AuthFrame>}
